<script>

//SHARED GLOBALS
var grid;
var GRID_SIZE = 20;
var GRID_HEIGHT = 10;
var VOXEL_SIZE = 50;

//RENDERING 
var container;
var camera, scene, renderer;
var canvas_width, canvas_height;
var theta = 45 * 0.5;

var projector, plane, cube;
var mouse2D, mouse3D, raycaster;
var shiftDown = false, ctrlDown = false;
var rollOveredFace;

var rollOverMesh, rollOverMaterial;

var currPos = new THREE.Vector3(), tmpVec = new THREE.Vector3(), normalMatrix = new THREE.Matrix3();

var cubeGeo, cubeMaterial;
var i, intersector;

var voxelPosition = new THREE.Vector3();

$(function(){
	$("#toolbox").organicTabs({ });
	if ( ! Detector.webgl ) 
		Detector.addGetWebGLMessage();

	// initialize grid with nulls
	grid = new Array(GRID_SIZE);
	for ( var x = 0; x < GRID_SIZE; x++ ) {
		grid[x] = new Array(GRID_SIZE);
		for ( var y = 0; y < GRID_SIZE; y++ ) {
			grid[x][y] = new Array(GRID_HEIGHT);
		}
	}

	//DISPLAY
	initGraphics();
	animate();
});

</script>
<div id='grid_container'>
</div>

<div id="toolbox">
					
	<ul class="nav">
		<li class="nav-one"><a href="#cat1" class="current">Category 1</a></li>
		<li class="nav-two"><a href="#cat2">Category 2</a></li>
		<li class="nav-three"><a href="#cat3">Category 3</a></li>
		<li class="nav-four"><a href="#cat4">Category 4</a></li>
	</ul>
    		
    <div class="list-wrap">
    	
    	<ul id="cat1">
    		Some text here.
    	</ul>
        		 
       	<ul id="cat2" class="hide">
       		More text
		</ul>

		<ul id="cat3" class="hide">
			More text
		</ul>

		<ul id="cat4" class="hide">
			More text
		</ul>

	</div> <!-- END List Wrap -->
</div> <!-- END toolbox  -->	

